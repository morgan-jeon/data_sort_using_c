```cpp
#include<cstdio>
#include<stack>
#include<queue>
#include<list>

int main(){

    std::stack<int> S;
    std::queue<int> Q;
    std::list<int> L;

    S.top()

    S.empty()
    S.size()

    S.push()
    S.pop()

    Q.front()
    Q.back()

    Q.empty()
    Q.size()

    Q.push(1)
    Q.pop()

    L.begin()
    L.end()

    L.empty()
    L.size()

    L.push_back()
    L.pop_back()
    L.push_front()
    L.pop_front()

    L.reverse()
}
```